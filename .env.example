# Authentication Backend URL
# Used for user authentication and OAuth flows
# Default: https://lokusmd.com (production)
# For local development, use: http://localhost:3000
VITE_AUTH_BASE_URL=https://lokusmd.com

# Google OAuth Credentials for Gmail Integration
#
# To set up Google OAuth:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Gmail API and Google+ API
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client IDs"
# 5. Application type: "Desktop application"
# 6. Add authorized redirect URI: http://localhost:8080/gmail-callback
# 7. Copy the Client ID and Client Secret below

GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Crash Reporting Configuration (Optional - GlitchTip/Sentry)
#
# Self-hosted crash reporting for monitoring production errors
# Leave empty to disable crash reporting (recommended for local development)
#
# To set up GlitchTip:
# 1. Deploy GlitchTip instance (see infrastructure/README.md)
# 2. Create a new project in GlitchTip dashboard
# 3. Copy the DSN from Project Settings
# 4. Add DSN to .env.production for production builds
#
# Format: https://<key>@<your-domain>/api/<project-id>/store/
VITE_SENTRY_DSN=
TAURI_SENTRY_DSN=

# Crash Reporting Environment (development/test/production)
VITE_SENTRY_ENVIRONMENT=development

# Enable/Disable Crash Reporting (true/false)
# Note: Users can opt-out in app Preferences regardless of this setting
VITE_ENABLE_CRASH_REPORTS=false

# Note: Copy this file to .env and replace with your actual credentials
# Never commit the actual .env file with real credentials to git